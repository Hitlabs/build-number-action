{
	"name": "pronto-build-number-action",
	"version": "1.0.0",
	"description": "GitHub action for capturing a build number with PR merge",
	"main": "index.js",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "MIT",
	"dependencies": {
		"@actions/core": "^1.6.0",
		"@actions/github": "^5.0.0",
		"axios": "^0.26.0"
	}
}
